# check.gov.ua API wrapper

```
pip install -r requirements.txt
python download_receipt_by_id.py monobank 5XKX-5PP6-PMC0-9C08
```

check.gov.ua may drop connection during the request to api/handler, just try again